package com.vtech.polymorphism.overriding;

public class ParentChildRunner {
	public static void main(String[] args) {
		Parent p = new Parent();
		p.marry();
		
		Parent c = new Child(); // reference is parent, but object is Child
		c.marry(); //Decided at runtime -> child marry()
	}

}
